[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pyspextool"
version = "0.0.1"
dependencies = [
    "astropy",
    "scipy",
    "matplotlib",
    "numpy",
]